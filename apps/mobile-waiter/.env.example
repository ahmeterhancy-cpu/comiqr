# ComiQR API base (host root — the app appends /v1 itself).
# Simülatör/emülatör: http://localhost:8000  (Android emülatörde http://10.0.2.2:8000)
# Fiziksel cihaz (aynı wifi): http://<bilgisayarının-LAN-IP'si>:8000  ör. http://192.168.1.20:8000
# Prod: https://<canlı-api-domaini>
EXPO_PUBLIC_API_URL=http://localhost:8000
